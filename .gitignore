# macOS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Vim
*.swp
*.swo
*~

# Local configs (machine/work-specific)
*.local
.zshrc.local
.gitconfig.local

# gh auth state (per-machine, may hold tokens) — keep config.yml, ignore hosts.yml
gh/.config/gh/hosts.yml

# Backup directories
*-backup-*/
backup/

# Neovim test/scratch files
nvim/.config/nvim/test-files/

# Claude Code (repo-local agent state only — the `claude/` stow package IS tracked)
/.claude/
.claude.json
.claude.json.backup
