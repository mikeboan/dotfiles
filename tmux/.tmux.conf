# Unbind default prefix
unbind C-b

# Set new prefix to C-a. Caps lock is mapped to control via system preferences.
set-option -g prefix C-a

# Bind C-a C-a to send C-a to the application (in case you need literal C-a)
bind-key C-a send-prefix

# <leader>r to reload conf
unbind r
bind r source-file ~/.tmux.conf

# vim-style pane navigation with <leader>h/j/k/l
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# vim-style pane resizing with <leader>H/J/K/L
bind -r H resize-pane -L 5
bind -r J resize-pane -D 5
bind -r K resize-pane -U 5
bind -r L resize-pane -R 5

# More intuitive pane split keybindings and open new window/pane at path of current window
bind | split-window -h -c "#{pane_current_path}"
bind - split-window -v -c "#{pane_current_path}"
unbind '"' # was vertial split
unbind % # was horizontal split

# vim-style copy mode
setw -g mode-keys vi
bind-key -T copy-mode-vi 'v' send -X begin-selection
bind-key -T copy-mode-vi 'y' send -X copy-selection-and-cancel

# Enable extended support for some more sophisticated terminal emulator
# Disable waiting time when pressing escape, for smoother Neovim usage. Disable
# if differentiation between function and meta keycombination is needed.
set-option -s escape-time 0

# Quality of life
set -g mouse on
set -g base-index 1 # Start counting windows from 1 instead of 0 for more intuitive quick switching.
setw -g pane-base-index 1
set -g renumber-windows on 
set -g history-limit 100000
set -g set-titles on 
set -g set-titles-string "#{pane_title}"

# List of plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'christoomey/vim-tmux-navigator'
set -g @plugin 'vaaleyard/tmux-dotbar'

# Configure tmux-dotbar UI
set -g @tmux-dotbar-bg "default"


# hijacking catppuccin with tokyonight storm colors""
# set -ogq @thm_bg "#24283b"
# set -ogq @thm_fg "#c0caf5"
# # Colors
# set -ogq @thm_rosewater "#b4f9f8"
# set -ogq @thm_flamingo "#ff9e64"
# set -ogq @thm_pink "#ff007c"
# set -ogq @thm_mauve "#bb9af7"
# set -ogq @thm_red "#f7768e"
# set -ogq @thm_maroon "#db4b4b"
# set -ogq @thm_peach "#ff9e64"
# set -ogq @thm_yellow "#e0af68"
# set -ogq @thm_green "#9ece6a"
# set -ogq @thm_teal "#7dcfff"
# set -ogq @thm_sky "#2ac3de"
# set -ogq @thm_sapphire "#0db9d7"
# set -ogq @thm_blue "#7aa2f7"
# set -ogq @thm_lavender "#89ddff"
# # Surfaces and overlays
# set -ogq @thm_subtext_1 "#a9b1d6"
# set -ogq @thm_subtext_0 "#c0caf5"
# set -ogq @thm_overlay_2 "#737aa2"
# set -ogq @thm_overlay_1 "#565f89"
# set -ogq @thm_overlay_0 "#545c7e"
# set -ogq @thm_surface_2 "#414868"
# set -ogq @thm_surface_1 "#3b4261"
# set -ogq @thm_surface_0 "#292e42"
# set -ogq @thm_mantle "#1f2335"
# set -ogq @thm_crust "#1b1e2d"


# bootstrap tpm
if "test ! -d ~/.tmux/plugins/tpm" \
   "run 'git clone https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm && ~/.tmux/plugins/tpm/bin/install_plugins'"

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run '~/.tmux/plugins/tpm/tpm'

