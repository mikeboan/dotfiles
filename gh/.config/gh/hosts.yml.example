# gh CLI per-host auth state — untracked (see .gitignore), generated by `gh auth login`.
# Example shape for reference:
#
# github.com:
#     git_protocol: https
#     users:
#         your-username:
#     user: your-username
